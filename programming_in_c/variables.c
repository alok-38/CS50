#include <stdio.h>

int main(void) {
    int age = 41;
    int age_in_months = age * 12;
    int age_in_days = age_in_months * 30;
    int grades[4];
    printf("%i\n", age);
    printf("%i\n", age_in_months);
    printf("%i\n", age_in_days);
    printf("%i\n", grades);
}